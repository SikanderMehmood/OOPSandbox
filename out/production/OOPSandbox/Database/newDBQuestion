
Inner join :
inner join gives you matching records from both the tables



union of two tables does not have duplicates
union all of two table have all values including duplicates


varchar
and

char -> it will always take fixed and string less than the fixed size will take the max size specified
