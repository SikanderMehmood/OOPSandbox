package CORE_JAVA.DataStructure.Collections.Queue;

//Elements of the priority queue are based on natural order or comparator given at queue construction
//Priority queue does not permit null values.
//A priority queue relaying on natural order does not permit insertion of non-comparable values

//In queue the first-In first out rule is implemented but in priority queue values are removed on the base of priority!

import Models.Book;
import java.util.Arrays;
import java.util.Comparator;
import java.util.SortedSet;
import java.util.TreeSet;

public class PriorityQueue {
//
//    Book book  = new Book("asda",1);
//    Book book1  = new Book("asda",1);
//    Book book2  = new Book("asda",1);
//    Book book3  = new Book("asda",1);
//    PriorityQueue queue = new PriorityQueue();
//    PriorityQueue queue1 = new PriorityQueue(Arrays.asList(book,book1,book2));
//    PriorityQueue  queue2  = new PriorityQueue(2);
//    PriorityQueue queue3 = new PriorityQueue(10, Comparator.naturalOrder());
//    PriorityQueue queue4 = new PriorityQueue(queue1);
//
//    SortedSet sortedSet = new TreeSet();
//    PriorityQueue queue5 = new PriorityQueue(sortedSet);
//
//
//    void someFunction(){
//
//        queue.add(book);
//        queue.contains(book);
//        queue.size();
//        queue.clear();
//        queue.remove(book);
//        queue.toArray();
//        queue.addAll(Arrays.asList(book,book2,book));
//        queue.peek();//retrieve head of queue but does not remove element from the queue.
//        queue.poll();//retrieve  head of queue and remove it from list
//
//    }




}
